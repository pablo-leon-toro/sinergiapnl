---
import titulo from "@assets/data/titulo-principal.json"
const tituloPrincipal = titulo[0];

import Layout from "../ui/layouts/MainLayout.astro";
import BtnPrincipal from "@components/atoms/BtnPrincipal.astro";
---

<Layout title={tituloPrincipal + " / 404 no encontrado"}>


    <div class="container">
        <h2>Contenido Inexistente</h2>
        <p>Lo sentimos, la página que buscas no existe. Por favor verifica la URL o vuelve a la página de inicio.</p>
        <BtnPrincipal href="/" body="Regresar al Inicio" /> 
    </div>
    
    
</Layout>

<style>
    div.container {
        padding: 2rem;
        width: 100%;
        height: calc(80dvh - var(--header-h));
        min-height: fit-content;
        color: var(--color-max-white);
        background: var(--gradient-blue-01);
        margin-left: auto;
        margin-right: auto;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    p {
        margin: 1rem 1rem 2rem 1rem;
    }
</style>
